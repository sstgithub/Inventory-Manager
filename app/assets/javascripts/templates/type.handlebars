<h2>{{metalName}}</h2>

<div>
	{{typeName}}
</div>

<div>
	<h5>Attributes</h5>
		<ul>
			{{#each attributes}}
				<li>{{attrName}}: {{attrValue}} 
					{{#if attrReq}}
						(Required)
					{{#else}}
						(Optional)
					{{/if}}
				</li>
			{{/each}}

<p>{{#link-to 'type.edit'}}Edit names{{/link-to}}</p>

<p>{{#link-to 'attributes' this}}Show attributes{{/link-to}}</p>

{{outlet}}