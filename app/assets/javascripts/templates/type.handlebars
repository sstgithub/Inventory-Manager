<h2>{{metalName}}</h2>

<div>
	{{typeName}}
</div>

<div>
	<h5>Attributes</h5>
		<ul>
			{{#each attribs}}
				<li>{{attrName}}: {{attrValue}} 
					{{!-- {{#if attrReq}}
						(Required)
					{{#else}}
						(Optional)
					{{/if}} --}}
				</li>
			{{/each}}
</div>

<p>{{#link-to 'type.edit'}}Edit names{{/link-to}}</p>

<p>{{#link-to 'attribs' this}}Show attributes{{/link-to}}</p>

{{outlet}}